// TODO: Create a variable called 'score' and assign it a number
let score = 70
// If the score is >= 70, log "Passing", else log "Failing"
if (score >= 70) {
    console.log("Passing");
    
} else {
    console.log("Failing");
    
}